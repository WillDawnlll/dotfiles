-- vim: ft=lua:ts=4:sw=4:et:ai:cin
-- xfce4/labwc local config for lean-conky-config
-- based on conky_lean_local.conf, scaled down for this setup

lcc.config = {
    scale = 1.2,
    width = 340,
    width_half = 110,
    spacing = 4,
}

lcc.fonts = {}

lcc.panel = {
    "datetime",
    { "cpu",     { top_n = 2 } },
    { "memory",  { top_n = 2 } },
    "network",
}

conky.config = {
    alignment = "top_right",
    gap_x = T_.sr(20),
    gap_y = T_.sr(5),
    font = T_ "terminess nerd font mono:size=$sc{10}",
    default_color = "white",
    color0 = "black",
    color1 = "cyan",
    color2 = "white",
    color3 = "cyan",

    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_argb_visual = true, own_window_argb_value = 64,

    default_graph_height = T_.sr(20),
    default_bar_height = T_.sr(5),
    store_graph_data_explicitly = false,
    out_to_wayland = true,
}
